<form method="post" action="{{ path('app_merchant_delete', {'id': merchant.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ merchant.id) }}">
    <button class="btn">Delete</button>
</form>
