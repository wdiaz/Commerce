{% extends 'base.html.twig' %}

{% block title %}Merchant index{% endblock %}

{% block body %}
    <h1>Merchant index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Uuid</th>
                <th>LocationX</th>
                <th>LocationY</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for merchant in merchants %}
            <tr>
                <td>{{ merchant.id }}</td>
                <td>{{ merchant.name }}</td>
                <td>{{ merchant.uuid }}</td>
                <td>{{ merchant.locationX }}</td>
                <td>{{ merchant.locationY }}</td>
                <td>
                    <a href="{{ path('app_admin_merchant_show', {'id': merchant.id}) }}">show</a>
                    <a href="{{ path('app_admin_merchant_edit', {'id': merchant.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="6">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_admin_merchant_new') }}">Create new</a>
{% endblock %}
